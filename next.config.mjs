/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactCompiler: true,
};

// module.exports = {
//   outputFileTracingRoot: __dirname,
// }

export default nextConfig;
